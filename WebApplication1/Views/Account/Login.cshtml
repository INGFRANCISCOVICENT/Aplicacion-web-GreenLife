@{
    ViewData["Title"] = "Iniciar Sesión";
}

<div class="login-page">
    <div class="login-card">
        <div class="text-center mb-4">
            <i class="fas fa-leaf text-success fa-3x mb-3"></i>
            <h1 class="login-brand">Bienvenido de nuevo</h1>
            <p class="login-title">Ingresa a tu cuenta de GreenLife</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger py-2" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i> @ViewBag.Error
            </div>
        }

        <form asp-action="Login" method="post">
            <div class="form-group mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" id="email" name="email" class="form-input" placeholder="ejemplo@correo.com" required />
            </div>
            <div class="form-group mb-4">
                <label for="password" class="form-label">Contraseña</label>
                <input type="password" id="password" name="password" class="form-input" placeholder="••••••••" required />
            </div>
            <div class="d-grid mb-3">
                <button type="submit" class="btn-auth">Iniciar Sesión</button>
            </div>
        </form>

        <div class="social-login-container">
            <div class="social-login-divider">
                <span>O continuar con</span>
            </div>
            <button type="button" class="btn-social btn-social-google" onclick="simulateSocialAuth('Google', this)">
                <i class="fab fa-google text-danger"></i>
                <span class="ms-2">Google</span>
            </button>
            <button type="button" class="btn-social btn-social-apple" onclick="simulateSocialAuth('Apple', this)">
                <i class="fab fa-apple"></i>
                <span class="ms-2">Apple</span>
            </button>
        </div>

        <div class="text-center mt-4 pt-3 border-top">
            <p class="mb-0 text-muted">¿No tienes cuenta? <a asp-action="Register" class="footer-link">Regístrate aquí</a></p>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function simulateSocialAuth(provider, btnElement) {
            // Save original content
            const originalHtml = btnElement.innerHTML;
            
            // Show loading state
            btnElement.innerHTML = `<i class="fas fa-spinner fa-spin"></i><span class="ms-2">Conectando con ${provider}...</span>`;
            btnElement.disabled = true;
            
            // Simulate network delay
            setTimeout(() => {
                alert(`[Simulación] Inicio de sesión exitoso con tu cuenta de ${provider}.`);
                // Restore button state
                btnElement.innerHTML = originalHtml;
                btnElement.disabled = false;
            }, 1500);
        }
    </script>
}
